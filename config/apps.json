/**
 * Settings relative to the thing apps.
 *
 * Usage:
 * -    apps_source (string): Source URL to download and update apps.
 * -    blacklist (array): List of app ids that should never be installed (black listed by the user)
 * 
 * Please, save kittens by respecting the alphabetical order in order to keep the settings organized.
 *
 */

{   
    "apps_source": process.cwd() + '/source/'
    "blackList": []
}
